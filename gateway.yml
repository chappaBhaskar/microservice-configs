server:
  port: 8080

spring:
  application:
    name: gateway

  cloud:
    gateway:
      routes:
        - id: userservice
          uri: lb://userservice
          predicates:
            - Path=/user/**

        - id: orderservice
          uri: lb://orderservice
          predicates:
            - Path=/order/**
